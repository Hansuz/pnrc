import 'package:flutter/material.dart';

class ActivitesWidget extends StatelessWidget {
  ActivitesWidget();

  @override
  Widget build(BuildContext context) {
    return Container(
      color: Colors.white,
      child: new Column(
        children: <Widget>[Text("activités")],
      ),
    );
  }
}
